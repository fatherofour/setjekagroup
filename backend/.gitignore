node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Uploaded vendor/compliance documents — local disk storage, not versioned
/uploads

# TypeScript incremental build cache — machine-local, regenerated on every
# build, not source
*.tsbuildinfo
